# Cloud Server Requirements
# Core Deep-Live-Cam dependencies
numpy>=1.23.5,<2
typing-extensions>=4.8.0
opencv-python==4.10.0.84
onnx==1.18.0
insightface==0.7.3
pillow==11.1.0
torch; sys_platform != 'darwin'
torch==2.5.1; sys_platform == 'darwin'
torchvision; sys_platform != 'darwin'
torchvision==0.20.1; sys_platform == 'darwin'
protobuf==4.25.1

# ONNX Runtime - CPU/GPU variants
onnxruntime-gpu==1.22.0; sys_platform != 'darwin'
onnxruntime-silicon==1.16.3; sys_platform == 'darwin' and platform_machine == 'arm64'
onnxruntime==1.22.0; sys_platform == 'darwin' and platform_machine != 'arm64'

# Web server dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0

# Optional: TensorFlow for some models
tensorflow; sys_platform != 'darwin'

# Optional: Additional security/monitoring
psutil==5.9.8